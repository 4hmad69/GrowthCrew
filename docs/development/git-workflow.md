# Solo-Developer Git Workflow

## Stable branch

`main` contains only work that has passed the milestone definition of done.

Do not perform experimental development directly on `main`.

## Feature branches

Use one feature branch per milestone.

Step 1 uses:

```text
feature/project-foundation